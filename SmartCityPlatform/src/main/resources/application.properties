# Spring Boot Application Properties
# Veritabanı Konfigürasyonu

# Server Port
server.port=8080

# Application Name
spring.application.name=Smart City Platform

# MySQL Veritabanı Bağlantı Bilgileri
spring.datasource.url=jdbc:mysql://localhost:3306/akilli_sehir_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@Lifesk26
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Ayarları (Eğer kullanılacaksa)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.root=INFO
logging.level.com.smartcity=DEBUG

# JSON Formatting
spring.jackson.serialization.indent-output=true

